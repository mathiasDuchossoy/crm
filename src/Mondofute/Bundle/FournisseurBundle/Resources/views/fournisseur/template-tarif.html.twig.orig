{{ form_row(tarif.prixPublic) }}
<h3>Périodes de validité</h3>
<ul class="periode_validites{{ name }}{{ index }}" data-prototype="{{ form_widget(tarif.periodeValidites.vars.prototype)|e('html_attr') }}">
    {% for periodeValidite in tarif.periodeValidites %}
        <li class="li_periode_validite">
            {{ form_widget(periodeValidite) }}
        </li>
    {% endfor %}
</ul>
<script>
<<<<<<< 8f9123aa6d8a5fb8f35dfb3d3ca557c729d24899

var collectionPeriodeValidite = function() {
    $collectionHolderperiode_validite{{ name }}{{ index }} = $('ul.periode_validites{{ name }}{{ index }}');
    $collectionHolderperiode_validite{{ name }}{{ index }}.find('.li_periode_validite').each(function() {
        addTagFormDeleteLink($(this));
    });
    $collectionHolderperiode_validite{{ name }}{{ index }}.append($newLinkLiperiode_validite{{ name }}{{ index }});
    $collectionHolderperiode_validite{{ name }}{{ index }}.data('index', $collectionHolderperiode_validite{{ name }}{{ index }}.find('.li_periode_validite').length);
    $addTagLinkperiode_validite{{ name }}{{ index }}.on('click', function (e) {
||||||| merged common ancestors
//    jQuery(document).ready(function() {
var test2 = function() {
    var $collectionHolderperiode_validite{{ index }};
    var $addTagLinkperiode_validite{{ prestationAnnexe.vars.id }} = $('<a href="#" class="add_periode_validite_link">Ajouter</a>');
    var $newLinkLiperiode_validite{{ index }} = $('<li></li>').append($addTagLinkperiode_validite{{ index }});
    $collectionHolderperiode_validite{{ index }} = $('ul.periode_validites{{ name }}{{ index }}');
    $collectionHolderperiode_validite{{ index }}.append($newLinkLiperiode_validite{{ index }});
    $collectionHolderperiode_validite{{ index }}.data('index', $collectionHolderperiode_validite{{ index }}.find('.li_periode_validite').length);
    $addTagLinkperiode_validite{{ index }}.on('click', function (e) {
=======
//    jQuery(document).ready(function() {
var test2 = function() {
    var $collectionHolderperiode_validite{{ index }};
    var $addTagLinkperiode_validite{{ index }} = $('<a href="#" class="add_periode_validite_link">Ajouter</a>');
    var $newLinkLiperiode_validite{{ index }} = $('<li></li>').append($addTagLinkperiode_validite{{ index }});
    $collectionHolderperiode_validite{{ index }} = $('ul.periode_validites{{ name }}{{ index }}');
    $collectionHolderperiode_validite{{ index }}.append($newLinkLiperiode_validite{{ index }});
    $collectionHolderperiode_validite{{ index }}.data('index', $collectionHolderperiode_validite{{ index }}.find('.li_periode_validite').length);
    $addTagLinkperiode_validite{{ index }}.on('click', function (e) {
>>>>>>> avancement sur les collection de la fiche des fournisseur/fournisseur-prestation-annexe
        e.preventDefault();
        addTagForm($collectionHolderperiode_validite{{ name }}{{ index }}, $newLinkLiperiode_validite{{ name }}{{ index }} , '__periode_validite_name__');
    });
}
if (window.jQuery){
<<<<<<< 8f9123aa6d8a5fb8f35dfb3d3ca557c729d24899
    var $collectionHolderperiode_validite{{ name }}{{ index }};
    var $addTagLinkperiode_validite{{ name }}{{ index }} = $('<a href="#" class="add_periode_validite_link">Ajouter</a>');
    var $newLinkLiperiode_validite{{ name }}{{ index }} = $('<li></li>').append($addTagLinkperiode_validite{{ name }}{{ index }});
    collectionPeriodeValidite();
    periodevaliditeDatetimePicker();
||||||| merged common ancestors
    test2();
}else{
    window.onload = test2;
=======
    test2();
>>>>>>> avancement sur les collection de la fiche des fournisseur/fournisseur-prestation-annexe
}
<<<<<<< 8f9123aa6d8a5fb8f35dfb3d3ca557c729d24899
||||||| merged common ancestors
//    });
=======
// else{
//    window.onload = test2;
//}
>>>>>>> avancement sur les collection de la fiche des fournisseur/fournisseur-prestation-annexe
</script>