{% extends 'base.html.twig' %}
{% block title %}
    {{ 'Liste des ModeleDescriptionForfaitSkis ' | capitalize }}
{% endblock %}
{% block body %}
    <h1>{{ 'liste des ModeleDescriptionForfaitSkis ' | capitalize }}</h1>

    {% include '::flashbags.html.twig' %}
    <table class="table-responsive table-striped table">
        <thead>
        <tr>
            <th>Id</th>
            {#<th>{{ 'libelle' | capitalize }}</th>#}
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for modeleDescriptionForfaitSki in modeleDescriptionForfaitSkis %}
            <tr>
                <td>
                    <a href="{{ path('modeledescriptionforfaitski_show', { 'id': modeleDescriptionForfaitSki.id }) }}">{{ modeleDescriptionForfaitSki.id }}</a>
                </td>
                {#<td>#}
                {#{% for domaine in modeleDescriptionForfaitSki.descriptionForfaitSki %}#}
                {#{% if domaine.site.crm == 1 %}#}
                {#{% for traduction in domaine.traductions %}#}
                {#{% if traduction.langue.code == app.request.locale %}#}
                {#{{ traduction.libelle }}#}
                {#{% endif %}#}
                {#{% endfor %}#}
                {#{% endif %}#}
                {#{% endfor %}#}
                {#</td>#}
                <td>
                    <ul class="list-unstyled">
                        <li>
                            <a class="btn btn-default"
                               href="{{ path('modeledescriptionforfaitski_show', { 'id': domaineUnifie.id }) }}">{{ 'visualiser' | capitalize }}</a>
                        </li>
                        <li>
                            <a class="btn btn-default"
                               href="{{ path('modeledescriptionforfaitski_edit', { 'id': domaineUnifie.id }) }}">{{ 'modifier' | capitalize }}</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div style="text-align: right">
        <ul class="list-unstyled">
            <li>
                <a class="btn btn-default"
                   href="{{ path('modeledescriptionforfaitski_new') }}">{{ 'ajouter' | capitalize }}</a>
            </li>
        </ul>
    </div>
{% endblock %}
