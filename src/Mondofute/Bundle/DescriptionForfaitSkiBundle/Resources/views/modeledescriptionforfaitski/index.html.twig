{% extends 'base.html.twig' %}

{% block body %}
    <h1>ModeleDescriptionForfaitSki list</h1>

    <table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for modeleDescriptionForfaitSki in modeleDescriptionForfaitSkis %}
            <tr>
                <td>
                    <a href="{{ path('modeledescriptionforfaitski_show', { 'id': modeleDescriptionForfaitSki.id }) }}">{{ modeleDescriptionForfaitSki.id }}</a>
                </td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('modeledescriptionforfaitski_show', { 'id': modeleDescriptionForfaitSki.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('modeledescriptionforfaitski_edit', { 'id': modeleDescriptionForfaitSki.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('modeledescriptionforfaitski_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
