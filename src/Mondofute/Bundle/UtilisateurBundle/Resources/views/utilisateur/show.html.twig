{% extends 'base.html.twig' %}

{% form_theme delete_form 'bootstrap_3_horizontal_layout.html.twig' %}
{% block title %}
    {{ 'visualisation du utilisateur' | capitalize }}
{% endblock %}
{% block body %}
    <h1>{{ 'Visualisation du utilisateur' | capitalize }}</h1>
    {% include '::flashbags.html.twig' %}
    <table class="table table-responsive table-striped">
        <tbody>
        <tr class="row">
            <th class="col-sm-1">Id</th>
            <td>{{ utilisateur.id }}</td>
        </tr>
        <tr class="row">
            <th class="col-sm-1">Login</th>
            <td>{{ utilisateur.username }}</td>
        </tr>
        <tr class="row">
            <th class="col-sm-1">Prenom / Nom</th>
            <td>{{ utilisateur.utilisateur.prenom ~ " / " ~ utilisateur.utilisateur.nom }}</td>
        </tr>
        {#<tr class="row">#}
        {#<th>Login</th>#}
        {#<td>#}
        {#{{ utilisateur.login }}#}
        {#</td>#}
        {#</tr>#}
        </tbody>
    </table>
    <div style="text-align: right">

        <ul class="list-unstyled">
            <li>
                <a href="{{ path('utilisateur_index') }}">Retour à la liste</a>
            </li>
            <li>
                <a class="btn btn-default"
                   href="{{ path('utilisateur_edit', { 'id': utilisateur.id }) }}">Modifier</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                {#<input type="submit" value="Delete">#}
                {{ form_end(delete_form) }}
            </li>
        </ul>
    </div>
{% endblock %}
