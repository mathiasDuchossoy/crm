{% extends 'base.html.twig' %}
{% block title %}
    {{ 'Liste des comment venir des stations' | capitalize }}
{% endblock %}
{% block body %}
    <h1>{{ 'liste des comment venir des stations' | capitalize }}</h1>
    {% include '::flashbags.html.twig' %}
    <table class="table-responsive table-striped table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for stationCommentVenirUnifie in stationCommentVenirUnifies %}
            <tr>
                <td>
                    <a href="{{ path('stationcommentvenir_show', { 'id': stationCommentVenirUnifie.id }) }}">{{ stationCommentVenirUnifie.id }}</a>
                </td>
                <td>
                    <ul class="list-unstyled">
                        <li>
                            <a class="btn btn-default"
                               href="{{ path('stationcommentvenir_show', { 'id': stationCommentVenirUnifie.id }) }}">{{ 'visualiser' | capitalize }}</a>
                        </li>
                        <li>
                            <a class="btn btn-default"
                               href="{{ path('stationcommentvenir_edit', { 'id': stationCommentVenirUnifie.id }) }}">{{ 'modifier' | capitalize }}</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div style="text-align: right">
        <ul class="list-unstyled">
            <li>
                <a class="btn btn-default"
                   href="{{ path('stationcommentvenir_new') }}">{{ 'ajouter' | capitalize }}</a>
            </li>
        </ul>
    </div>
{% endblock %}
