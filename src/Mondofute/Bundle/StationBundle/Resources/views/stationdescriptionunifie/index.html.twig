{% extends 'base.html.twig' %}
{% block title %}
    {{ 'Liste de stationDescriptions' | capitalize }}
{% endblock %}
{% block body %}
    <h1>{{ 'liste des stationDescriptions' | capitalize }}</h1>
    {% include '::flashbags.html.twig' %}
    <table class="table-responsive table-striped table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for stationDescriptionUnifie in stationDescriptionUnifies %}
            <tr>
                <td>
                    <a href="{{ path('stationdescription_show', { 'id': stationDescriptionUnifie.id }) }}">{{ stationDescriptionUnifie.id }}</a>
                </td>
                {#<td>#}
                {#{% for stationDescription in stationDescriptionUnifie.stationDescriptions %}#}
                {#{% if stationDescription.site.crm == 1 %}#}
                {#{% for traduction in stationDescription.traductions %}#}
                {#{% if traduction.langue.code == app.request.locale %}#}
                {#{{ traduction.libelle }}#}
                {#{% endif %}#}
                {#{% endfor %}#}
                {#{% endif %}#}
                {#{% endfor %}#}
                {#</td>#}
                <td>
                    <ul class="list-unstyled">
                        <li>
                            <a class="btn btn-default"
                               href="{{ path('stationdescription_show', { 'id': stationDescriptionUnifie.id }) }}">{{ 'visualiser' | capitalize }}</a>
                        </li>
                        <li>
                            <a class="btn btn-default"
                               href="{{ path('stationdescription_edit', { 'id': stationDescriptionUnifie.id }) }}">{{ 'modifier' | capitalize }}</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div style="text-align: right">
        <ul class="list-unstyled">
            <li>
                <a class="btn btn-default" href="{{ path('stationdescription_new') }}">{{ 'ajouter' | capitalize }}</a>
            </li>
        </ul>
    </div>
{% endblock %}
