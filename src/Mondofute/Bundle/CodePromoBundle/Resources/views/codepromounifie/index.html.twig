{% extends 'base.html.twig' %}

{% block body %}
    <h1>CodePromoUnifie list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for codePromoUnifie in codePromoUnifies %}
            <tr>
                <td><a href="{{ path('codepromo_show', { 'id': codePromoUnifie.id }) }}">{{ codePromoUnifie.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('codepromo_show', { 'id': codePromoUnifie.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('codepromo_edit', { 'id': codePromoUnifie.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('codepromo_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
