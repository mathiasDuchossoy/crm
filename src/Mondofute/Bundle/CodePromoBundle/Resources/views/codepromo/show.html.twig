{% extends 'base.html.twig' %}

{% block body %}
    <h1>CodePromo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ codePromo.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('codepromo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('codepromo_edit', { 'id': codePromo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
